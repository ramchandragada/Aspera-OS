# Remaster notes

`scripts/build-iso.sh` downloads Linux Mint XFCE, unsquashes the live filesystem,
applies purge/install/branding hooks, then rebuilds a hybrid ISO.

Pinned Mint version: see `MINT_ISO_URL` in `scripts/build-iso.sh`.
